.env
__pycache__/
*.pyc
.myenv/
myenv/
node_modules/
dist/
.DS_Store
# data/seed is COMMITTED on purpose: the seeded users, rules and trust config
# are part of the deployment contract (flask seed needs them on any host).
uploads/
*.log
.pytest_cache/