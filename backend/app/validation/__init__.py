# backend/app/validation/__init__.py
"""Validation engine: DSL evaluator (dsl.py) + scope runners (runner.py).

Named `app.validation` alongside `app.models.validation` the same way
`app.ingestion` sits alongside `app.models.ingestion` -- service package and
its tables, one per module.
"""
